import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
from datetime import date
import math

from dataset import Dataset

class DataInsight():
    def __init__():
        self.dataset = Dataset()

    def draw_attribute_adr(attribute):
        attribute = train_df.loc[:, 'total_of_special_requests']
        adr = 
        plt.plot(attribute, adr, ',')
        plt.ylim(0, 1000)
        plt.show()

    def draw_attribute_is_canceled():
        pass